"use client";
import Link from "next/link";
import { ArrowRight, Instagram, Mail, MessageCircle } from "lucide-react";
import { Button, GlassCard } from "@/components/ui";
import { useChannels } from "@/lib/api";
export default function ChannelsOnboardingPage() { const { data = [] } = useChannels(); return <main className="min-h-screen bg-[linear-gradient(rgba(255,255,255,.045)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,.045)_1px,transparent_1px)] bg-[size:56px_56px] px-6 py-8"><header className="mx-auto flex max-w-7xl justify-between text-textSecondary"><strong className="text-[#c1c1ff]">Qchart <span className="ml-4 text-textMuted">Setup</span></strong><span>Step 2 of 4</span></header><section className="mx-auto mt-32 max-w-5xl text-center"><h1 className="gradient-text text-5xl font-bold md:text-6xl">Connect Your Communications</h1><p className="mx-auto mt-5 max-w-3xl text-xl text-textSecondary">Link your messaging channels to Qchart.</p><div className="mt-12 grid gap-5 md:grid-cols-2">{data.map((c) => <GlassCard key={c.id} className="p-6 text-left"><div className="flex items-start justify-between"><div className="flex gap-4"><div className="grid size-14 place-items-center rounded-2xl border border-cyan/30 bg-cyan/10 text-cyan">{c.kind === "instagram" ? <Instagram /> : c.kind === "gmail" ? <Mail /> : <MessageCircle />}</div><div><h2 className="text-2xl font-bold">{c.label}</h2><p className={c.connected ? "text-success" : "text-warning"}>{c.connected ? "Direct Integration" : "API Key Required"}</p></div></div></div><p className="mt-6 text-textSecondary">Auto-sync messages and route leads into the unified inbox.</p></GlassCard>)}</div><div className="mt-10 flex justify-between"><Link href="/onboarding/workspace"><Button variant="ghost">Back</Button></Link><Link href="/onboarding/integrations"><Button>Continue <ArrowRight className="ml-2 inline" /></Button></Link></div></section></main>; }
