"use client";
import { KeyRound, Moon, Shield, User } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Button, GlassCard, SectionHeader } from "@/components/ui";
export default function SettingsPage() { return <AppShell><SectionHeader title="Settings" description="Control profile, security, integrations, and theme preferences." /><section className="grid gap-6 xl:grid-cols-2"><Panel icon={<User />} title="Profile"><input className="w-full rounded-2xl bg-white px-4 py-4 text-slate-700" defaultValue="Alex Mercer" /><input className="mt-4 w-full rounded-2xl bg-white px-4 py-4 text-slate-700" defaultValue="alex@qchart.demo" /><Button className="mt-4">Save Profile</Button></Panel><Panel icon={<Moon />} title="Appearance"><p className="text-textSecondary">Use the sidebar toggle to switch dark and light mode.</p></Panel><Panel icon={<Shield />} title="Security"><p className="text-textSecondary">Supabase Auth owns identity. Gateway services validate bearer claims before data access.</p></Panel><Panel icon={<KeyRound />} title="API Keys"><p className="text-textSecondary">Provider keys stay server-side. WhatsApp and OpenAI can be configured after launch.</p></Panel></section></AppShell>; }
function Panel({ icon, title, children }: { icon: React.ReactNode; title: string; children: React.ReactNode }) { return <GlassCard className="p-6"><h2 className="mb-5 flex gap-3 text-2xl font-bold text-cyan">{icon}{title}</h2>{children}</GlassCard>; }
